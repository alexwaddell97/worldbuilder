---
plan: 04-02
phase: 04-content-editor-tiptap-wikilinks
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/lib/tiptap/wikilink-extension.ts
  - src/components/tiptap/wikilink-node.tsx
autonomous: true
requirements:
  - CONTENT-EDITOR-03
  - CONTENT-EDITOR-04
  - CONTENT-EDITOR-05

must_haves:
  truths:
    - "WikilinkExtension is a Tiptap Node that stores id+label as attrs, never plain text"
    - "Wikilink nodes survive markdown serialization round-trips via addMarkdownParser + addMarkdownSerializer"
    - "WikilinkNodeView renders a pill; dead links (dead=true) render with a visible broken-link indicator"
    - "No dangerouslySetInnerHTML is used anywhere in the node view"
  artifacts:
    - path: "src/lib/tiptap/wikilink-extension.ts"
      provides: "WikilinkExtension Tiptap Node with markdown handlers"
      exports: ["WikilinkExtension"]
    - path: "src/components/tiptap/wikilink-node.tsx"
      provides: "WikilinkNodeView React component"
      exports: ["WikilinkNodeView"]
  key_links:
    - from: "src/lib/tiptap/wikilink-extension.ts"
      to: "src/components/tiptap/wikilink-node.tsx"
      via: "addNodeView returning ReactNodeViewRenderer(WikilinkNodeView)"
      pattern: "ReactNodeViewRenderer"
    - from: "src/lib/tiptap/wikilink-extension.ts"
      to: "@tiptap/extension-markdown"
      via: "addMarkdownParser + addMarkdownSerializer storage hooks"
      pattern: "addMarkdownParser|addMarkdownSerializer|markdownSerializer"
---

<objective>
Build the WikilinkExtension Tiptap Node and its React NodeView component. This is the custom ProseMirror extension that stores wikilinks as structured data (id, label, dead) instead of plain text, and renders them as interactive pills in the editor.

Purpose: The extension is the core data model for wikilinks — all autocomplete insertion, markdown serialization, and dead-link rendering depend on this extension being correct.
Output: WikilinkExtension (tiptap Node.create) + WikilinkNodeView (React component) as two standalone files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/lib/db/schema.ts
@.planning/phases/04-content-editor-tiptap-wikilinks/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WikilinkExtension — Node.create() with attrs, HTML, and markdown handlers</name>
  <read_first>
    - node_modules/@tiptap/core/dist/index.js — skim for Node.create() signature and storage pattern (do NOT assume API — read before writing)
    - node_modules/@tiptap/extension-markdown/dist/index.js — look for how existing extensions register addMarkdownParser and addMarkdownSerializer; find the storage key used (e.g., `this.storage.markdown` pattern or `Markdown.config.extensions`)
    - node_modules/@tiptap/suggestion/dist/index.js — skim for the Suggestion plugin function signature: what options it takes (char, items, render, command)
  </read_first>
  <files>
    src/lib/tiptap/wikilink-extension.ts
  </files>
  <action>
    Create `src/lib/tiptap/wikilink-extension.ts` as a `"use client"` file (it uses browser-only ProseMirror APIs).

    Import from `@tiptap/core`: `Node`, `mergeAttributes`, `RawCommands`. Import `Suggestion` from `@tiptap/suggestion`. Import `ReactNodeViewRenderer` from `@tiptap/react`. Import `WikilinkNodeView` from `@/components/tiptap/wikilink-node` (forward reference — file created in Task 2).

    Export `WikilinkExtension = Node.create({...})` with:

    **name:** `"wikilink"`

    **group:** `"inline"` — **inline:** `true` — **atom:** `true` — **selectable:** `true`

    **attrs:** `{ id: { default: null }, label: { default: "" }, dead: { default: false } }`

    **parseHTML:** `[{ tag: 'span[data-wikilink="true"]', getAttrs: (el) => ({ id: el.getAttribute("data-id"), label: el.getAttribute("data-label"), dead: el.getAttribute("data-dead") === "true" }) }]`

    **renderHTML:** `({ HTMLAttributes }) => ["span", mergeAttributes(HTMLAttributes, { "data-wikilink": "true", "data-id": HTMLAttributes.id, "data-label": HTMLAttributes.label, "data-dead": String(HTMLAttributes.dead ?? false) }), 0]` — note: use `0` (hole) so ProseMirror knows this is a leaf atom with content slot.

    Actually since `atom: true`, renderHTML should render as a self-contained element. The correct pattern for an atom node is to NOT include `0` as a hole. Use: `["span", mergeAttributes({ "data-wikilink": "true", "data-id": node.attrs.id, "data-label": node.attrs.label, "data-dead": String(node.attrs.dead ?? false) })]`

    **addNodeView:** `() => ReactNodeViewRenderer(WikilinkNodeView)`

    **addMarkdownSerializer:** Register via the extension-markdown storage convention. Check what the Markdown extension exposes for serializer registration. If it uses `this.storage`, add: `addStorage() { return { markdown: { serialize(state, node) { state.write(\`[[${node.attrs.label}|${node.attrs.id}]]\`); }, parse: { ... } } } }`. If the extension-markdown uses a different registration hook (e.g., `addMarkdownParser` option at configure time), use that pattern. The serialized format is `[[label|id]]` (label first, pipe separator, UUID second).

    **addMarkdownParser:** Register a tokenizer rule that matches `\[\[([^\]|]+)\|([a-f0-9-]{36})\]\]` (label and UUID). On match, emit a `wikilink` inline node token with attrs `{ label: match[1], id: match[2], dead: false }`.

    **addInputRules:** Return empty array `[]` — autocomplete is handled by the Suggestion plugin, not input rules.

    **addProseMirrorPlugins:** Return `[Suggestion({ editor: this.editor, char: "[[", items: async ({ query }) => { ... } /* populated by TiptapEditor via extension.configure() */, command: ({ editor, range, props }) => { editor.chain().focus().deleteRange(range).insertContent({ type: "wikilink", attrs: { id: props.id, label: props.name, dead: false } }).run(); }, render: () => ({ /* populated by TiptapEditor */ }) })]` — use empty stubs for items and render that the TiptapEditor will fill via `WikilinkExtension.configure({ suggestion: { items, render } })`.

    Export a TypeScript type: `export type WikilinkAttrs = { id: string; label: string; dead: boolean }`.

    Ensure all imports are from `@tiptap/*` packages (installed in Plan 01). Do NOT use `dangerouslySetInnerHTML` anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "wikilink-extension|error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/tiptap/wikilink-extension.ts` exists and exports `WikilinkExtension` and `WikilinkAttrs`
    - `WikilinkExtension` has `name: "wikilink"`, `inline: true`, `atom: true`
    - attrs include `id`, `label`, `dead`
    - `addNodeView` returns `ReactNodeViewRenderer(WikilinkNodeView)`
    - Markdown serialization produces `[[label|id]]` format
    - Markdown parser matches `[[label|UUID]]` pattern and restores wikilink node
    - No `dangerouslySetInnerHTML` in this file
    - `npx tsc --noEmit` exits 0 (no errors in this file)
  </acceptance_criteria>
  <done>WikilinkExtension defined as a Tiptap Node with id/label/dead attrs, HTML parseHTML/renderHTML, markdown serializer/parser, Suggestion plugin stub, and ReactNodeViewRenderer wired</done>
</task>

<task type="auto">
  <name>Task 2: WikilinkNodeView — React NodeViewWrapper rendering wikilink pill + dead-link state</name>
  <read_first>
    - node_modules/@tiptap/react/dist/index.js — look for NodeViewWrapper and NodeViewContent exports and the props shape passed to a React NodeView (especially: `node`, `selected`, `updateAttributes`, `extension`)
    - src/lib/tiptap/wikilink-extension.ts — confirm WikilinkAttrs shape (id, label, dead) from Task 1
    - src/components/entities/entity-card.tsx — reference for Tailwind class conventions used in this codebase (badge-style pill patterns)
  </read_first>
  <files>
    src/components/tiptap/wikilink-node.tsx
  </files>
  <action>
    Create `src/components/tiptap/wikilink-node.tsx` as `"use client"`.

    Import `NodeViewWrapper` from `@tiptap/react`. Import `cn` from `@/lib/utils`. Import `WikilinkAttrs` from `@/lib/tiptap/wikilink-extension`.

    Export `WikilinkNodeView` as a React functional component. Props are the standard Tiptap NodeViewWrapper props — type as `{ node: { attrs: WikilinkAttrs }; selected: boolean }` (the full NodeViewProps type from `@tiptap/react` can be imported and used for correctness).

    Render:
    ```
    <NodeViewWrapper as="span" className="inline">
      <span
        className={cn(
          "inline-flex items-center gap-0.5 px-1.5 py-0.5 rounded text-xs font-medium cursor-pointer select-none",
          "ring-1 ring-inset transition-colors",
          node.attrs.dead
            ? "bg-destructive/10 text-destructive ring-destructive/30 line-through"
            : "bg-primary/10 text-primary ring-primary/20 hover:bg-primary/20",
          selected && "ring-2 ring-primary"
        )}
        data-wikilink-node
        contentEditable={false}
      >
        {node.attrs.dead && <span aria-label="broken link" className="mr-0.5">⚠</span>}
        {node.attrs.label || node.attrs.id}
      </span>
    </NodeViewWrapper>
    ```

    The component must NOT use `dangerouslySetInnerHTML`. All text rendering uses JSX string interpolation. The `contentEditable={false}` attribute is required on atom node views to prevent ProseMirror from treating the inner content as editable.

    The `dead` state visually signals a broken link: `line-through` text decoration, destructive color, warning icon. The live state renders as a primary-colored pill badge.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "wikilink-node|error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/tiptap/wikilink-node.tsx` exists and exports `WikilinkNodeView`
    - Component uses `NodeViewWrapper` from `@tiptap/react` (not a plain div)
    - `contentEditable={false}` is set on the inner rendered element
    - No `dangerouslySetInnerHTML` in this file
    - Dead-link state applies `line-through` class and renders a `⚠` indicator
    - Live-link state renders with `bg-primary/10 text-primary` pill styling
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>WikilinkNodeView renders as a pill for live links and a broken-link indicator for dead=true; uses NodeViewWrapper; no dangerouslySetInnerHTML; zero TypeScript errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tiptap JSON attrs → React render | node.attrs.label / node.attrs.id from stored jsonb content rendered in JSX |
| Markdown string → parseMarkdown | User-typed `[[…]]` patterns parsed into wikilink nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | XSS | WikilinkNodeView renders label | mitigate | label rendered via JSX `{node.attrs.label}` — React escapes all string output; no dangerouslySetInnerHTML |
| T-04-06 | XSS | WikilinkNodeView renders id | mitigate | id rendered as data attribute only via mergeAttributes — not rendered as HTML content |
| T-04-07 | Tampering | Markdown parser regex | accept | [[label|UUID]] pattern requires valid UUID-like format; mismatched patterns are silently skipped by Tiptap's tokenizer |
| T-04-SC | Tampering | npm packages already audited | accept | Packages audited in Plan 01 — no new installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 for both new files
- `src/lib/tiptap/wikilink-extension.ts`: grep `WikilinkExtension` and `WikilinkAttrs` both exported
- `src/components/tiptap/wikilink-node.tsx`: grep `WikilinkNodeView` exported; grep confirms `contentEditable={false}` present; grep confirms no `dangerouslySetInnerHTML`
- `src/lib/tiptap/wikilink-extension.ts`: grep `atom: true` and `inline: true` present
</verification>

<success_criteria>
- WikilinkExtension: inline atom node, attrs {id, label, dead}, parseHTML/renderHTML, addMarkdownParser (parses [[label|UUID]]), addMarkdownSerializer (emits [[label|UUID]]), addNodeView uses ReactNodeViewRenderer
- WikilinkNodeView: NodeViewWrapper, pill styling, dead=true shows ⚠ + line-through + destructive color, live shows primary pill, contentEditable={false}, no dangerouslySetInnerHTML
- Zero TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/04-content-editor-tiptap-wikilinks/04-02-SUMMARY.md` when done
</output>
