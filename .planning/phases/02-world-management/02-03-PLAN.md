---
phase: 02-world-management
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/worlds/world-form.tsx
  - src/components/worlds/create-world-dialog.tsx
  - src/components/worlds/edit-world-dialog.tsx
  - src/components/worlds/delete-world-dialog.tsx
  - src/components/worlds/privacy-toggle.tsx
  - src/components/worlds/world-card.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: false
requirements: [UAT-1, UAT-2, UAT-3, UAT-4]
must_haves:
  truths:
    - "Builder sees a dashboard listing all worlds they own, each as a card"
    - "Builder can open a Create World dialog, submit name + description, and see the new world"
    - "Builder can edit a world's name/description from the card action menu"
    - "Builder can delete a world via an AlertDialog confirmation"
    - "Builder can toggle a world between private and public with instant feedback"
    - "Empty dashboard shows a 'No worlds yet' state with a create CTA"
    - "Invalid input (empty name) shows an inline field error in the dialog"
  artifacts:
    - path: "src/components/worlds/world-form.tsx"
      provides: "shared create/edit form using useActionState with inline Zod errors + live slug preview"
      contains: "useActionState"
    - path: "src/components/worlds/create-world-dialog.tsx"
      provides: "Create World dialog wrapping WorldForm with createWorldAction"
      contains: "createWorldAction"
    - path: "src/components/worlds/edit-world-dialog.tsx"
      provides: "Edit World dialog binding updateWorldAction to world id"
      contains: "updateWorldAction"
    - path: "src/components/worlds/delete-world-dialog.tsx"
      provides: "AlertDialog confirmation calling deleteWorldAction"
      contains: "deleteWorldAction"
    - path: "src/components/worlds/privacy-toggle.tsx"
      provides: "Switch with useOptimistic calling togglePrivacyAction"
      contains: "useOptimistic"
    - path: "src/components/worlds/world-card.tsx"
      provides: "Dashboard card: name, slug, description, privacy badge, action menu, toggle"
      contains: "DropdownMenu"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "RSC dashboard querying worlds by owner, rendering card grid or empty state"
      contains: "getWorldsByOwner"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "getWorldsByOwner"
      via: "owner-scoped query in RSC"
      pattern: "getWorldsByOwner\\("
    - from: "src/components/worlds/world-form.tsx"
      to: "createWorldAction/updateWorldAction"
      via: "useActionState"
      pattern: "useActionState\\("
    - from: "src/components/worlds/world-card.tsx"
      to: "EditWorldDialog/DeleteWorldDialog/PrivacyToggle"
      via: "composed in card"
      pattern: "PrivacyToggle"
---

<objective>
Build the world-management UI on the dashboard: the shared create/edit form, the three dialogs (create, edit, delete), the privacy toggle, the world card, and the dashboard page that lists all of a user's worlds.

Purpose: Delivers the user-visible CRUD surface for UAT-1 through UAT-4 — create, list, edit, delete, and privacy toggle. Consumes the Server Actions from 02-01 and the shadcn primitives from 02-02.
Output: Six components under `src/components/worlds/` plus the rewritten `src/app/(app)/dashboard/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-world-management/02-RESEARCH.md
@.planning/phases/02-world-management/02-PATTERNS.md
@.planning/phases/02-world-management/02-UI-SPEC.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WorldForm + Create/Edit dialogs with useActionState and live slug preview</name>
  <files>src/components/worlds/world-form.tsx, src/components/worlds/create-world-dialog.tsx, src/components/worlds/edit-world-dialog.tsx</files>
  <read_first>
    - .planning/phases/02-world-management/02-PATTERNS.md (`world-form.tsx`, `create-world-dialog.tsx`, `edit-world-dialog.tsx` sections — useActionState replacement of the login useState pattern, dialog open/close state, exact imports)
    - .planning/phases/02-world-management/02-UI-SPEC.md (Surface Spec 3 Create dialog, 4 Edit dialog — field list, placeholders, slug preview styling, footer button labels; Copywriting Contract for exact strings)
    - .planning/phases/02-world-management/02-RESEARCH.md (Pattern 3 useActionState form)
    - src/app/(auth)/login/page.tsx (closest analog: form field + Label/Input stack, error display `text-sm text-destructive`, pending button label change)
    - src/lib/actions/worlds.ts (createWorldAction, updateWorldAction signatures from 02-01)
    - src/lib/validations/worlds.ts (WorldActionState shape from 02-01)
    - src/components/ui/dialog.tsx and src/components/ui/textarea.tsx (from 02-02)
  </read_first>
  <action>
    Create `src/components/worlds/world-form.tsx` (`"use client"`). Accept props `action` (the bound Server Action), `initialValues?: { name: string; description: string }`, `submitLabel` ("Create World" | "Save Changes"), `pendingLabel` ("Creating..." | "Saving..."), `onSuccess?: () => void`. Use `const [state, formAction, pending] = useActionState(action, { errors: {} })`. Render a `<form action={formAction}>` with: a Label+Input for `name` (`name="name"`, placeholder `"e.g. The Shattered Realm"`, `autoFocus`, `maxLength={100}`, defaultValue from initialValues); a Label+Textarea for `description` (`name="description"`, placeholder `"What is this world about? (optional)"`, `rows={3}`, `maxLength={500}`, defaultValue); a read-only live slug preview rendered as `bg-muted rounded-md px-3 py-2 text-sm font-mono text-muted-foreground` showing `/worlds/{slugify-preview-of-name}` (derive client-side from the controlled name value using a lightweight inline slug transform — lowercase, spaces→hyphens, strip non-alphanumerics; this is preview-only, server is source of truth). Per-field errors below each field: `state.errors?.name` / `state.errors?.description` rendered `text-sm text-destructive mt-1` with `id` wired via `aria-describedby`. Footer with a Discard `variant="outline"` button (calls onSuccess to close) and a submit `variant="default"` button `disabled={pending}` showing pendingLabel when pending. When `state.message` indicates success (edit path), call `onSuccess()` via an effect.

    Create `src/components/worlds/create-world-dialog.tsx` (`"use client"`): local `useState(open)`, shadcn `Dialog` with `DialogTrigger` = primary Button (`Plus` icon 16px + "Create World"), `DialogContent className="max-w-md"`, title "Create a new world", description "Give your world a name. A URL slug is generated automatically.", body = `<WorldForm action={createWorldAction} submitLabel="Create World" pendingLabel="Creating..." onSuccess={() => setOpen(false)} />`. (createWorldAction redirects on success, so onSuccess mainly handles the discard path.)

    Create `src/components/worlds/edit-world-dialog.tsx` (`"use client"`): accept `world: World` prop and a controlled `open`/`onOpenChange` pair (so the card's dropdown can trigger it). Title "Edit world", description "Update your world's name or description. The slug cannot be changed after creation." Bind the action: `updateWorldAction.bind(null, world.id)`. Pass `initialValues={{ name: world.name, description: world.description ?? "" }}`, submitLabel "Save Changes", pendingLabel "Saving...". The slug preview shows the existing immutable slug (read-only, not derived from name on edit).
  </action>
  <verify>
    <automated>grep -q 'useActionState' src/components/worlds/world-form.tsx && grep -q 'createWorldAction' src/components/worlds/create-world-dialog.tsx && grep -q 'updateWorldAction' src/components/worlds/edit-world-dialog.tsx && grep -q 'aria-describedby' src/components/worlds/world-form.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `world-form.tsx` uses `useActionState`, renders name Input + description Textarea + live `font-mono` slug preview, and per-field error elements wired with `aria-describedby`
    - Submit button shows `pendingLabel` when `pending` is true
    - `create-world-dialog.tsx` triggers from a primary "Create World" Button with `Plus` icon and binds `createWorldAction`
    - `edit-world-dialog.tsx` binds `updateWorldAction.bind(null, world.id)` and pre-fills name/description; slug shown as immutable
    - Exact copy strings from UI-SPEC Copywriting Contract present (dialog titles, descriptions, button labels)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Shared form and create/edit dialogs work with inline errors, pending states, and live slug preview.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DeleteWorldDialog, PrivacyToggle, and WorldCard composition</name>
  <files>src/components/worlds/delete-world-dialog.tsx, src/components/worlds/privacy-toggle.tsx, src/components/worlds/world-card.tsx</files>
  <read_first>
    - .planning/phases/02-world-management/02-PATTERNS.md (`delete-world-dialog.tsx`, `privacy-toggle.tsx`, `world-card.tsx` sections — AlertDialog imports, useOptimistic + useTransition pattern, lucide icon imports, cn() conditional classes, named export)
    - .planning/phases/02-world-management/02-UI-SPEC.md (Surface Spec 2 World Card, 5 Delete confirmation, 6 Privacy toggle; Interaction States; Accessibility Contract for aria-labels)
    - src/lib/actions/worlds.ts (deleteWorldAction, togglePrivacyAction signatures from 02-01)
    - src/lib/db/schema.ts (World type to type the card prop)
    - src/components/worlds/edit-world-dialog.tsx (Task 1 — the EditWorldDialog the card composes)
    - src/components/ui/alert-dialog.tsx, src/components/ui/switch.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/ui/dropdown-menu.tsx (from 02-02)
  </read_first>
  <action>
    Create `src/components/worlds/delete-world-dialog.tsx` (`"use client"`): controlled `open`/`onOpenChange` props plus `world: World`. shadcn `AlertDialog`; title "Delete world?"; description "This will permanently delete <strong>{world.name}</strong> and all its content. This action cannot be undone." (`max-w-sm`); footer with `AlertDialogCancel` labeled "Keep world" (`variant="outline"`) and an action button labeled "Delete world" styled destructive (`bg-destructive text-destructive-foreground`), `disabled={pending}` → "Deleting..." Use `useTransition`; in the click handler `startTransition(async () => { await deleteWorldAction(world.id); })` (the action redirects to /dashboard). Single confirmation click — no type-to-confirm.

    Create `src/components/worlds/privacy-toggle.tsx` (`"use client"`): props `worldId: string`, `isPublic: boolean`. `const [optimistic, setOptimistic] = useOptimistic(isPublic)` and `const [pending, startTransition] = useTransition()`. Render shadcn `Switch` `checked={optimistic}`, `disabled={pending}`, `aria-label="Toggle world privacy"`; on change `startTransition(async () => { setOptimistic(!optimistic); await togglePrivacyAction(worldId); })`.

    Create `src/components/worlds/world-card.tsx` (`"use client"` — needs local dialog open state; export named `WorldCard({ world }: { world: World })`). Use shadcn `Card` (`p-6`, `hover:shadow-sm transition-shadow`). Header row (`flex items-start justify-between`): left = name (`text-base font-semibold`) + slug (`font-mono text-sm text-muted-foreground` showing `/worlds/{world.slug}`, wrapped in a span with `aria-label="World URL: /worlds/{slug}"`); right = `DropdownMenu` with an icon-only ghost Button trigger (`MoreHorizontal` 16px, `aria-label="World options for {world.name}"`, `h-8 w-8`). Dropdown items: "Edit" (`Pencil` 14px) → opens EditWorldDialog; "Delete" (`Trash2` 14px, `text-destructive`) → opens DeleteWorldDialog. Content: description (`text-sm text-muted-foreground line-clamp-2 mb-4`). Footer row (`flex items-center gap-2`): privacy `Badge variant="outline"` (Private = `Lock` 12px + "Private"; Public = `Globe` 12px + "Public", both `text-muted-foreground` — no color change), `flex-1` spacer, then `PrivacyToggle`. Manage `editOpen`/`deleteOpen` local state and render the two controlled dialogs. The card heading wraps the name in a `Link` to `/worlds/{world.slug}` (navigates to detail page built in 02-04).
  </action>
  <verify>
    <automated>grep -q 'useOptimistic' src/components/worlds/privacy-toggle.tsx && grep -q 'deleteWorldAction' src/components/worlds/delete-world-dialog.tsx && grep -q 'DropdownMenu' src/components/worlds/world-card.tsx && grep -q 'aria-label="Toggle world privacy"' src/components/worlds/privacy-toggle.tsx && grep -q 'line-clamp-2' src/components/worlds/world-card.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `delete-world-dialog.tsx` uses AlertDialog, shows world name in `<strong>`, cancel "Keep world", confirm "Delete world"→"Deleting...", calls `deleteWorldAction(world.id)`
    - `privacy-toggle.tsx` uses `useOptimistic` + `useTransition`, Switch `aria-label="Toggle world privacy"`, calls `togglePrivacyAction`
    - `world-card.tsx` renders Card with name, `font-mono` slug, `line-clamp-2` description, outline privacy Badge (Lock/Globe icons), DropdownMenu (Edit/Delete), and composes PrivacyToggle + Edit/Delete dialogs
    - DropdownMenu trigger has `aria-label="World options for {name}"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Delete dialog, privacy toggle, and the composed world card are complete with correct a11y and copy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Dashboard page — owner-scoped world list + empty state</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(app)/dashboard/page.tsx (current file — preserve `p-8`, heading typography; replace placeholder copy)
    - .planning/phases/02-world-management/02-PATTERNS.md (`dashboard/page.tsx` section — header row flex, grid classes, CreateWorldDialog as right CTA)
    - .planning/phases/02-world-management/02-UI-SPEC.md (Surface Spec 1 Dashboard; Copywriting Contract empty state strings)
    - .planning/phases/02-world-management/02-RESEARCH.md (Pitfall 5 — dashboard staleness; `dynamic = 'force-dynamic'` is the chosen mitigation for this project's no-`'use cache'` posture)
    - src/lib/db/queries/worlds.ts (getWorldsByOwner from 02-01)
    - src/app/(app)/layout.tsx (session pattern — but note layout already guards auth; page still fetches session for ownerId)
    - src/components/worlds/world-card.tsx and create-world-dialog.tsx (from Tasks 1-2)
  </read_first>
  <action>
    Rewrite `src/app/(app)/dashboard/page.tsx` as an async RSC. At module scope export `export const dynamic = "force-dynamic"` — per RESEARCH Pitfall 5, this project does NOT enable `'use cache'`, so the dashboard must bypass the Page Cache and read fresh DB state on every request. This is the chosen mitigation for post-mutation staleness: the `updateTag(...)` calls in the 02-01 Server Actions have no matching `cacheTag` annotation (none exists), so `force-dynamic` — not tag invalidation — is what guarantees a freshly-created/edited/deleted world is reflected immediately. Do NOT add `cacheTag` to the query or page.

    Fetch session via `auth.api.getSession({ headers: await headers() })`; if null `redirect("/login")` (belt-and-suspenders; proxy + layout already guard). Call `getWorldsByOwner(session.user.id)`. Keep the page container `p-8`. Header row: `flex items-center justify-between mb-6` with `<h1 className="text-2xl font-semibold tracking-tight">Your Worlds</h1>` on the left and `<CreateWorldDialog />` on the right. If worlds array is empty, render a centered empty state: heading "No worlds yet" (`text-lg font-semibold`), body "Create your first world to start building your universe." (`text-sm text-muted-foreground`), and an inline `<CreateWorldDialog />` CTA. Otherwise render `<div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">` mapping each world to `<WorldCard key={world.id} world={world} />`. Remove the old "World management comes in Phase 2" placeholder text.
  </action>
  <verify>
    <automated>grep -q 'getWorldsByOwner' "src/app/(app)/dashboard/page.tsx" && grep -q 'CreateWorldDialog' "src/app/(app)/dashboard/page.tsx" && grep -q 'WorldCard' "src/app/(app)/dashboard/page.tsx" && grep -q 'No worlds yet' "src/app/(app)/dashboard/page.tsx" && grep -q 'force-dynamic' "src/app/(app)/dashboard/page.tsx" && ! grep -q 'comes in Phase 2' "src/app/(app)/dashboard/page.tsx" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Page exports `dynamic = "force-dynamic"` (dashboard never serves a stale cached world list — RESEARCH Pitfall 5)
    - Dashboard calls `getWorldsByOwner(session.user.id)` — owner-scoped
    - Renders `CreateWorldDialog` in header row and a responsive `grid ... sm:grid-cols-2 lg:grid-cols-3` of `WorldCard`
    - Empty state shows "No worlds yet" + "Create your first world to start building your universe." + a create CTA
    - Old placeholder copy removed
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Dashboard lists all of the user's worlds as cards (or an empty state) with a working create CTA; `force-dynamic` keeps it fresh after mutations; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify world CRUD flow in the browser</name>
  <what-built>Dashboard world list, create/edit/delete dialogs, privacy toggle. All Server Actions are auth- and owner-scoped (02-01) and the UI is wired to them.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and sign in, then visit `/dashboard`.
    2. Empty state: with no worlds, confirm "No worlds yet" + a "Create World" button appear.
    3. Create: click "Create World", type a name (watch the live `/worlds/{slug}` preview update), optionally a description, submit. Confirm you land on `/worlds/{slug}` (detail page comes from 02-04 — a 404 here is acceptable until 02-04 ships; the world should still appear on /dashboard).
    4. Validation: open Create again, submit with an empty name → confirm inline error "World name is required."
    5. List: confirm the new world appears as a card with name, `/worlds/...` slug, privacy badge.
    6. Edit: card menu → Edit, change the name, Save Changes → confirm the card updates after the dialog closes.
    7. Privacy toggle: flip the Switch → confirm the badge/state updates optimistically without a full reload.
    8. Delete: card menu → Delete → confirm AlertDialog shows the world name in bold; click "Delete world" → confirm the card disappears.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → Server Action | Form fields submitted to createWorldAction/updateWorldAction |
| client dialog → delete/toggle action | worldId passed to deleteWorldAction/togglePrivacyAction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | client-rendered slug preview | accept | Preview is cosmetic only; the server (02-01 createWorldAction) generates the authoritative slug via slugify + uniqueness check. Client preview divergence has no security impact |
| T-02-08 | Information Disclosure | WorldCard rendered with arbitrary world | mitigate | Cards only render worlds returned by `getWorldsByOwner(session.user.id)` — owner-scoped query from 02-01; no other user's worlds reach the client |
| T-02-01 | Tampering | delete/edit/toggle worldId from client | mitigate | Inherited from 02-01 — every action re-verifies session and scopes the mutation by ownerId; UI cannot escalate |
| T-02-SC | Tampering | npm installs | accept | No package installs in this plan (primitives installed in 02-02) |
</threat_model>

<verification>
- `npm run build` exits 0
- Human checkpoint (Task 4) confirms create/list/edit/delete/toggle flows
- Covers UAT-1 (create), UAT-2 (list), UAT-3 (edit), UAT-4 (delete with confirmation)
</verification>

<success_criteria>
- Dashboard lists owner's worlds; empty state present; `force-dynamic` prevents post-mutation staleness
- Create/edit/delete dialogs functional with inline validation and pending states
- Privacy toggle gives optimistic feedback
- Build passes; human verification approved
</success_criteria>

<output>
Create `.planning/phases/02-world-management/02-03-SUMMARY.md` when done
</output>
