---
phase: 02-world-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/migrations/
  - src/lib/validations/worlds.ts
  - src/lib/db/queries/worlds.ts
  - src/lib/actions/worlds.ts
autonomous: false
requirements: [UAT-1, UAT-3, UAT-4, UAT-5]
must_haves:
  truths:
    - "Builder can create a world; row is inserted scoped to their user id"
    - "Builder can edit a world's name and description"
    - "Builder can delete a world they own"
    - "Two different users can each have a world with the same slug"
    - "A user cannot create two worlds with the same slug"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "worlds table with composite (owner_id, slug) unique constraint"
      contains: "worlds_owner_id_slug_unique"
    - path: "drizzle/migrations/"
      provides: "migration dropping global slug unique, adding composite unique"
      contains: "worlds_owner_id_slug_unique"
    - path: "src/lib/validations/worlds.ts"
      provides: "Zod v4 CreateWorldSchema, UpdateWorldSchema, WorldActionState type"
      exports: ["CreateWorldSchema", "UpdateWorldSchema", "WorldActionState"]
    - path: "src/lib/db/queries/worlds.ts"
      provides: "getWorldsByOwner, getWorldBySlug owner-scoped read helpers"
      exports: ["getWorldsByOwner", "getWorldBySlug"]
    - path: "src/lib/actions/worlds.ts"
      provides: "createWorldAction, updateWorldAction, deleteWorldAction, togglePrivacyAction"
      exports: ["createWorldAction", "updateWorldAction", "deleteWorldAction", "togglePrivacyAction"]
  key_links:
    - from: "src/lib/actions/worlds.ts"
      to: "auth.api.getSession"
      via: "session verification at top of every action"
      pattern: "auth\\.api\\.getSession"
    - from: "src/lib/actions/worlds.ts"
      to: "worlds table mutations"
      via: "owner-scoped where clause"
      pattern: "eq\\(worlds\\.ownerId"
---

<objective>
Build the server-side data foundation for world CRUD: migrate the slug uniqueness constraint to be per-user, define Zod validation schemas, owner-scoped read query helpers, and the four Server Actions (create, update, delete, toggle privacy).

Purpose: Every world UI in later plans consumes these contracts. The schema migration is BLOCKING — globally-unique slugs break the per-user uniqueness UAT (UAT-5) and cause cross-user collisions.
Output: `src/lib/db/schema.ts` (migrated), a new Drizzle migration, `src/lib/validations/worlds.ts`, `src/lib/db/queries/worlds.ts`, `src/lib/actions/worlds.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-world-management/02-RESEARCH.md
@.planning/phases/02-world-management/02-PATTERNS.md
@.planning/phases/02-world-management/02-VALIDATION.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: [BLOCKING] Migrate worlds.slug to composite (owner_id, slug) unique constraint</name>
  <files>src/lib/db/schema.ts, drizzle/migrations/</files>
  <read_first>
    - src/lib/db/schema.ts (current worlds + entityTypes definitions — entityTypes already uses the two-argument table-level `unique()` pattern to copy)
    - drizzle/migrations/0000_loud_cable.sql (confirms existing `worlds_slug_unique` constraint name to drop)
    - drizzle.config.ts (migration output path `./drizzle/migrations`, reads DATABASE_URL from .env.local)
    - .planning/phases/02-world-management/02-RESEARCH.md ("Critical Schema Migration" section — exact before/after definition + expected SQL)
    - .planning/phases/02-world-management/02-VALIDATION.md (Manual-Only Verifications row UAT-5 — the migration-applied + two-user manual confirmation this task feeds)
    - node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md (only if any Next.js API touched — this task is schema-only)
  </read_first>
  <action>
    Modify the `worlds` `pgTable` in src/lib/db/schema.ts: remove `.unique()` from the `slug` column (leave `.notNull()`), convert the single-argument `pgTable("worlds", {...})` form to the two-argument form `pgTable("worlds", {...}, (table) => ({ ... }))`, and add a table-level composite unique named exactly `worlds_owner_id_slug_unique` on `(table.ownerId, table.slug)`. Use `unique("worlds_owner_id_slug_unique").on(table.ownerId, table.slug)` — mirror the existing `entityTypes` `worldSlugUnique` pattern. The `unique` import is already present. Do NOT change any other columns or the `$onUpdate` hook on `updatedAt`. Preserve all existing type exports (`World`, `NewWorld`).

    Then generate and apply the migration: run `npx drizzle-kit generate` (produces a new `.sql` in drizzle/migrations/ containing `DROP CONSTRAINT "worlds_slug_unique"` and `ADD CONSTRAINT "worlds_owner_id_slug_unique" UNIQUE("owner_id","slug")`), then run `npx drizzle-kit migrate` to apply against Neon. drizzle-kit reads DATABASE_URL from .env.local via dotenv in drizzle.config.ts. If `drizzle-kit migrate` prompts interactively or fails because DATABASE_URL is unset, STOP and surface the blocker — do not fabricate migration success.

    MIGRATION-APPLIED IS NOT GREP-VERIFIABLE: the `<automated>` block below greps source/migration files only and will pass even if `drizzle-kit migrate` never ran against Neon (Neon uses the HTTP driver — there is no local `psql` and no portable in-CI database assertion for this project). Therefore the executor MUST capture the literal stdout/exit code of `npx drizzle-kit migrate` in the SUMMARY and confirm it exited 0 applying the new migration. The downstream proof that the constraint is live on the database is the UAT-5 two-user manual test recorded in 02-VALIDATION.md (Manual-Only Verifications) — exercised at phase end, not here. Record that this manual step is pending in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q 'worlds_owner_id_slug_unique' src/lib/db/schema.ts && ! grep -E 'slug.*\.unique\(\)' src/lib/db/schema.ts && grep -rq 'worlds_owner_id_slug_unique' drizzle/migrations/ && grep -rq 'DROP CONSTRAINT' drizzle/migrations/ && npx tsc --noEmit</automated>
    <manual>Confirm `npx drizzle-kit migrate` exited 0 against Neon (paste the command's stdout/exit status into the SUMMARY). The grep gate above does NOT prove the migration was applied to the database — only that the source and migration file are correct. Live-constraint confirmation is the UAT-5 two-user test in 02-VALIDATION.md, run at phase end.</manual>
  </verify>
  <acceptance_criteria>
    - `src/lib/db/schema.ts` contains `unique("worlds_owner_id_slug_unique").on(` with `table.ownerId` and `table.slug`
    - `src/lib/db/schema.ts` slug column no longer has `.unique()` (only `.notNull()`)
    - A new file in `drizzle/migrations/` contains both `DROP CONSTRAINT "worlds_slug_unique"` and `ADD CONSTRAINT "worlds_owner_id_slug_unique" UNIQUE("owner_id","slug")`
    - `npx tsc --noEmit` exits 0
    - `npx drizzle-kit migrate` exited 0 applying the new migration against Neon, with the command output recorded in the SUMMARY (blocker surfaced if DB unreachable — do NOT fabricate)
    - SUMMARY notes that the live per-user-uniqueness proof (UAT-5) is the pending two-user manual test in 02-VALIDATION.md, not this grep gate
  </acceptance_criteria>
  <done>Schema defines per-user composite slug uniqueness, migration generated and confirmed applied to Neon (migrate exit 0 recorded), type check clean; UAT-5 live verification deferred to the documented manual two-user test.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Zod validation schemas and owner-scoped query helpers</name>
  <files>src/lib/validations/worlds.ts, src/lib/db/queries/worlds.ts</files>
  <read_first>
    - .planning/phases/02-world-management/02-RESEARCH.md (Pattern 1 Zod v4 schema; "World Dashboard Query" code example — verified Drizzle API; Pitfall 5 — caching posture)
    - .planning/phases/02-world-management/02-PATTERNS.md (`src/lib/validations/worlds.ts` and `src/lib/db/queries/worlds.ts` sections — exact import shapes and query bodies)
    - .planning/phases/02-world-management/02-UI-SPEC.md (Copywriting Contract — exact error strings)
    - src/lib/db/index.ts (the `db` export to import)
    - src/lib/db/schema.ts (the `worlds` table and `World` type to import)
  </read_first>
  <action>
    Create `src/lib/validations/worlds.ts`. Import `z` from `zod`. Define `CreateWorldSchema` and `UpdateWorldSchema` as `z.object` with: `name` = `z.string().min(1, { error: "World name is required." }).max(100, { error: "Name must be 100 characters or fewer." })`, `description` = `z.string().max(500, { error: "Description must be 500 characters or fewer." }).optional()`. Use the Zod v4 `{ error: ... }` key (NOT `{ message: ... }`). Export inferred types `CreateWorldInput`, `UpdateWorldInput`. Export a `WorldActionState` type: `{ errors?: { name?: string[]; description?: string[] }; message?: string }`.

    Create `src/lib/db/queries/worlds.ts`. Import `db` from `@/lib/db`, `worlds` from `@/lib/db/schema`, and `eq, and, desc` from `drizzle-orm`. Export `getWorldsByOwner(ownerId: string)` returning `db.select().from(worlds).where(eq(worlds.ownerId, ownerId)).orderBy(desc(worlds.updatedAt))`. Export `getWorldBySlug(slug: string, ownerId: string)` returning the single row (or null) from `db.select().from(worlds).where(and(eq(worlds.slug, slug), eq(worlds.ownerId, ownerId))).limit(1)`. Both queries MUST be owner-scoped — never query by slug alone.

    CACHING POSTERA — DO NOT annotate these queries with `cacheTag`/`'use cache'`. Per RESEARCH Pitfall 5, this project does not enable the `'use cache'` directive in Phase 2. The dashboard and detail pages that consume these helpers will instead export `dynamic = 'force-dynamic'` (see 02-03 Task 3 and 02-04) so each render reads fresh DB state — bypassing the Page Cache entirely. The `updateTag(...)` calls in the Server Actions (02-01 Task 3) are therefore defensive read-your-writes hooks that become load-bearing only if `'use cache'`/`cacheTag` is adopted in a later phase; with `force-dynamic` pages they are harmless no-ops and the dashboard never goes stale after a mutation. Leave a one-line comment in this file noting "no cacheTag — consumers use dynamic='force-dynamic' (RESEARCH Pitfall 5)".
  </action>
  <verify>
    <automated>grep -q 'error:' src/lib/validations/worlds.ts && ! grep -q 'message:' src/lib/validations/worlds.ts && grep -q 'WorldActionState' src/lib/validations/worlds.ts && grep -q 'getWorldsByOwner' src/lib/db/queries/worlds.ts && grep -q 'eq(worlds.ownerId' src/lib/db/queries/worlds.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/validations/worlds.ts` exports `CreateWorldSchema`, `UpdateWorldSchema`, `CreateWorldInput`, `UpdateWorldInput`, `WorldActionState`
    - All Zod messages use `{ error: ... }`; no `{ message: ... }` present
    - `src/lib/db/queries/worlds.ts` exports `getWorldsByOwner` and `getWorldBySlug`
    - `getWorldBySlug` where clause combines slug AND ownerId (IDOR-safe); never slug alone
    - No `cacheTag`/`'use cache'` in queries.ts; comment documents the `force-dynamic` consumer posture
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Validation schemas and owner-scoped read helpers exist and type-check; caching posture documented (consumers use force-dynamic).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: World Server Actions (create, update, delete, togglePrivacy) with auth + IDOR guards</name>
  <files>src/lib/actions/worlds.ts</files>
  <read_first>
    - .planning/phases/02-world-management/02-RESEARCH.md (Pattern 1 createWorldAction, Pattern 4 generateUniqueSlug, Pattern 5 updateTag, Pitfall 5 caching posture, "Delete World Action" example, Security Domain section)
    - .planning/phases/02-world-management/02-PATTERNS.md (`src/lib/actions/worlds.ts` section — file header/import block, auth guard, IDOR-safe mutation patterns, cache invalidation)
    - src/app/(app)/layout.tsx (server-side `auth.api.getSession({ headers: await headers() })` pattern)
    - src/lib/validations/worlds.ts (the schemas + WorldActionState created in Task 2)
    - src/lib/db/schema.ts (worlds columns to insert/update)
    - node_modules/next/dist/docs/01-app/02-guides/forms.md (Server Action + useActionState contract; updateTag usage)
  </read_first>
  <action>
    Create `src/lib/actions/worlds.ts` with the `"use server"` directive at the top. Import `z`/schemas from `@/lib/validations/worlds`, `headers` from `next/headers`, `redirect` from `next/navigation`, `updateTag` and `revalidatePath` from `next/cache`, `auth` from `@/lib/auth`, `db` from `@/lib/db`, `worlds` from `@/lib/db/schema`, `eq, and, like` from `drizzle-orm`, and `slugify` from `slugify`.

    Implement a private `async function generateUniqueSlug(name, ownerId)`: `slugify(name, { lower: true, strict: true })` for the base, query existing slugs for this owner with `like(worlds.slug, base + "%")` scoped by `eq(worlds.ownerId, ownerId)`, return base if free else append `-2`, `-3`, ... until unique (per RESEARCH Pattern 4).

    `createWorldAction(prevState: WorldActionState, formData: FormData): Promise<WorldActionState>` — verify session (`auth.api.getSession({ headers: await headers() })`, throw "Unauthorized" if null); `CreateWorldSchema.safeParse({ name, description })`; on failure return `{ errors: validated.error.flatten().fieldErrors }`; generate unique slug; `db.insert(worlds).values({ name, description: description ?? null, slug, ownerId: session.user.id, isPublic: false })`; call `updateTag(\`worlds-${session.user.id}\`)` then `revalidatePath("/dashboard")`; `redirect(\`/worlds/${slug}\`)`.

    `updateWorldAction(worldId: string, prevState: WorldActionState, formData: FormData)` — verify session; `UpdateWorldSchema.safeParse`; on failure return field errors; `db.update(worlds).set({ name, description: description ?? null }).where(and(eq(worlds.id, worldId), eq(worlds.ownerId, session.user.id)))` (IDOR-safe — both id AND ownerId); slug is NOT regenerated on edit; `updateTag(\`worlds-${session.user.id}\`)`, `updateTag(\`world-${worldId}\`)`, `revalidatePath("/dashboard")`; return `{ message: "saved" }` (no redirect — dialog closes client-side).

    `deleteWorldAction(worldId: string)` — verify session; `db.delete(worlds).where(and(eq(worlds.id, worldId), eq(worlds.ownerId, session.user.id)))`; `updateTag(\`worlds-${session.user.id}\`)`, `revalidatePath("/dashboard")`; `redirect("/dashboard")`.

    `togglePrivacyAction(worldId: string)` — verify session; fetch current `isPublic` for the owned world (scoped by id AND ownerId); `db.update(worlds).set({ isPublic: !current }).where(and(eq(worlds.id, worldId), eq(worlds.ownerId, session.user.id)))`; `updateTag(\`world-${worldId}\`)`, `updateTag(\`worlds-${session.user.id}\`)`, `revalidatePath("/dashboard")`.

    Every action verifies the session and trusts ONLY `session.user.id` for ownerId — never accept an ownerId from the client. Do NOT use `revalidateTag` (single-arg form is a TypeScript error in Next.js 16); `updateTag` is correct for read-your-writes and `revalidatePath` drops the client Router Cache so the dashboard re-renders after navigation. Note: per RESEARCH Pitfall 5 the consuming pages export `dynamic = 'force-dynamic'`, so the `updateTag(...)` calls are defensive (no matching `cacheTag` exists yet) — keep them so the actions remain correct if `'use cache'` is adopted later; with `force-dynamic` the dashboard already reads fresh state on every render.
  </action>
  <verify>
    <automated>grep -q "'use server'" src/lib/actions/worlds.ts || grep -q '"use server"' src/lib/actions/worlds.ts; grep -q 'auth.api.getSession' src/lib/actions/worlds.ts && [ "$(grep -c 'eq(worlds.ownerId' src/lib/actions/worlds.ts)" -ge 3 ] && ! grep -E 'revalidateTag\(' src/lib/actions/worlds.ts && grep -q 'createWorldAction' src/lib/actions/worlds.ts && grep -q 'togglePrivacyAction' src/lib/actions/worlds.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `"use server"` directive
    - Exports `createWorldAction`, `updateWorldAction`, `deleteWorldAction`, `togglePrivacyAction`
    - Every action calls `auth.api.getSession` and throws/guards when session is null
    - `update`, `delete`, and `togglePrivacy` mutations all include `eq(worlds.ownerId, session.user.id)` in their where clause (at least 3 occurrences of `eq(worlds.ownerId`)
    - No single-argument `revalidateTag(` call anywhere (Next.js 16 rule)
    - `createWorldAction` returns `{ errors }` on Zod failure and redirects on success
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All four owner-scoped Server Actions exist with auth + IDOR guards and correct Next.js 16 cache invalidation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | FormData and worldId arguments are attacker-controlled until validated |
| Server Action → DB | All mutations cross into persistent state; must be owner-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | updateWorldAction / deleteWorldAction / togglePrivacyAction worldId arg | mitigate | Every mutation where clause includes `and(eq(worlds.id, worldId), eq(worlds.ownerId, session.user.id))` — IDOR prevented; a non-owner's worldId matches zero rows |
| T-02-02 | Spoofing | session.user.id source | mitigate | ownerId always read from `auth.api.getSession`; client-passed ownerId never trusted |
| T-02-03 | Tampering | FormData mass assignment | mitigate | Zod schema allowlist accepts only `name` and `description`; `isPublic`/`slug`/`ownerId` set server-side only |
| T-02-04 | Tampering | slug injection via name | mitigate | `slugify(name, { strict: true })` strips unsafe chars; composite unique constraint enforces per-user uniqueness at DB layer |
| T-02-05 | Information Disclosure | unauthenticated action invocation | mitigate | Session verified at top of every action; throws "Unauthorized" when null |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — all packages (zod, slugify) already vetted [OK] in RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after all tasks
- Schema migration generated AND `npx drizzle-kit migrate` confirmed exit 0 against Neon (recorded in SUMMARY; blocker surfaced otherwise)
- UAT-5 live proof is the two-user manual test in 02-VALIDATION.md Manual-Only Verifications (deferred to phase end): create two users, each create a world named "Test" → both succeed with slug `test`; same user's second "Test" → `test-2`
</verification>

<success_criteria>
- worlds table enforces `UNIQUE(owner_id, slug)`; migration generated and applied (migrate exit 0 recorded)
- Four Server Actions exist, each session-verified and owner-scoped
- Zod schemas use Zod v4 `{ error }` key with exact UI-SPEC copy
- Query helpers are owner-scoped (IDOR-safe); caching posture (force-dynamic consumers) documented
</success_criteria>

<output>
Create `.planning/phases/02-world-management/02-01-SUMMARY.md` when done
</output>
