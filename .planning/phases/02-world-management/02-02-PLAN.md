---
phase: 02-world-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/alert-dialog.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/switch.tsx
  - package.json
autonomous: true
requirements: [UAT-1, UAT-2, UAT-3, UAT-4]
must_haves:
  truths:
    - "All shadcn primitives needed by Phase 2 UI exist under src/components/ui"
    - "Each primitive imports from @/lib/utils cn() and matches the neutral baseColor preset"
  artifacts:
    - path: "src/components/ui/card.tsx"
      provides: "Card, CardHeader, CardContent, CardFooter primitives"
      exports: ["Card", "CardHeader", "CardContent"]
    - path: "src/components/ui/dialog.tsx"
      provides: "Dialog, DialogContent, DialogHeader, DialogFooter, DialogTitle, DialogDescription, DialogTrigger"
      exports: ["Dialog", "DialogContent", "DialogTrigger"]
    - path: "src/components/ui/alert-dialog.tsx"
      provides: "AlertDialog primitives for destructive confirmation"
      exports: ["AlertDialog", "AlertDialogAction", "AlertDialogCancel"]
    - path: "src/components/ui/textarea.tsx"
      provides: "Textarea primitive"
      exports: ["Textarea"]
    - path: "src/components/ui/badge.tsx"
      provides: "Badge primitive with outline variant"
      exports: ["Badge"]
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "DropdownMenu primitives for world card action menu"
      exports: ["DropdownMenu", "DropdownMenuItem", "DropdownMenuTrigger"]
    - path: "src/components/ui/separator.tsx"
      provides: "Separator primitive"
      exports: ["Separator"]
    - path: "src/components/ui/switch.tsx"
      provides: "Switch primitive for privacy toggle"
      exports: ["Switch"]
  key_links:
    - from: "src/components/ui/*.tsx"
      to: "@/lib/utils cn"
      via: "className merge helper"
      pattern: "from \"@/lib/utils\""
---

<objective>
Install the eight shadcn/ui primitives required by Phase 2 world-management UI: card, dialog, alert-dialog, textarea, badge, dropdown-menu, separator, switch.

Purpose: Plans 02-03 and 02-04 build the dashboard, dialogs, and world detail shell on top of these primitives. Installing them in a dedicated parallel plan (no file overlap with 02-01) keeps Wave 1 fast.
Output: Eight new files under `src/components/ui/`, plus their Radix peer dependencies in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-world-management/02-RESEARCH.md
@.planning/phases/02-world-management/02-UI-SPEC.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install shadcn primitives via CLI with manual copy-paste fallback</name>
  <files>src/components/ui/card.tsx, src/components/ui/dialog.tsx, src/components/ui/alert-dialog.tsx, src/components/ui/textarea.tsx, src/components/ui/badge.tsx, src/components/ui/dropdown-menu.tsx, src/components/ui/separator.tsx, src/components/ui/switch.tsx, package.json</files>
  <read_first>
    - components.json (shadcn registry config — style=default, baseColor=neutral, cssVariables=true; confirms aliases `@/components/ui` and `@/lib/utils`)
    - src/components/ui/button.tsx (existing shadcn primitive — confirms the exact cn() import path `@/lib/utils`, cva usage, and the project's component style so new primitives match)
    - .planning/phases/02-world-management/02-UI-SPEC.md ("To Add via shadcn" table — the eight component names; Color section confirms neutral palette / outline badge variant)
    - .planning/phases/01-project-foundation/01-SUMMARY.md (Deviation #2 — shadcn `init` CLI fails under Tailwind v4 CSS-first config; manual copy-paste is the known fallback)
  </read_first>
  <action>
    Run `npx shadcn@latest add card dialog alert-dialog textarea badge dropdown-menu separator switch` in one command. This pulls each component file into `src/components/ui/` and installs the Radix peer deps (`@radix-ui/react-dropdown-menu`, `@radix-ui/react-separator`, `@radix-ui/react-switch` — `@radix-ui/react-dialog` and `@radix-ui/react-alert-dialog` are already in package.json).

    Tailwind v4 fallback (Phase 1 confirmed `shadcn init` fails under CSS-first config): if `shadcn add` errors with a Tailwind config / registry error, manually create each file by copying the component source from ui.shadcn.com for the `default` style / `neutral` baseColor, ensuring each imports `cn` from `@/lib/utils` (match button.tsx exactly), and manually `npm install` the three missing Radix packages: `@radix-ui/react-dropdown-menu @radix-ui/react-separator @radix-ui/react-switch`. Do NOT introduce any third-party registry — official shadcn registry only (UI-SPEC Registry Safety).

    Do not modify globals.css or components.json — the design tokens and `@theme inline` bridge are already established in Phase 1. After install, verify the Badge default/outline variant exists (UI-SPEC uses `variant="outline"` for the privacy badge).
  </action>
  <verify>
    <automated>for f in card dialog alert-dialog textarea badge dropdown-menu separator switch; do test -f "src/components/ui/$f.tsx" || { echo "MISSING $f"; exit 1; }; done; grep -lq 'from "@/lib/utils"' src/components/ui/card.tsx && grep -q '@radix-ui/react-switch' package.json && grep -q '@radix-ui/react-dropdown-menu' package.json && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All eight files exist: `src/components/ui/{card,dialog,alert-dialog,textarea,badge,dropdown-menu,separator,switch}.tsx`
    - `src/components/ui/card.tsx` imports `cn` from `@/lib/utils`
    - `package.json` lists `@radix-ui/react-switch`, `@radix-ui/react-dropdown-menu`, and `@radix-ui/react-separator`
    - `src/components/ui/badge.tsx` defines an `outline` variant (used by privacy badge)
    - No third-party registry referenced in any added file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Eight shadcn primitives installed, Radix peer deps present, type check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | New Radix packages installed during `shadcn add` cross a supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | shadcn add / npm install (Radix peers) | accept | `@radix-ui/react-dialog` + `@radix-ui/react-alert-dialog` already vetted [OK] in RESEARCH Package Legitimacy Audit; `@radix-ui/react-dropdown-menu`, `@radix-ui/react-separator`, `@radix-ui/react-switch` are first-party @radix-ui scope packages (same publisher, same ~3yr provenance) pulled by the official shadcn registry — no [ASSUMED]/[SUS] verdicts |
| T-02-06 | Tampering | third-party component registry | mitigate | UI-SPEC Registry Safety declares official shadcn registry only; action forbids any third-party registry |
</threat_model>

<verification>
- All eight primitives present and type-checking
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Eight shadcn/ui primitives available for consumption by 02-03 and 02-04
- Radix peer dependencies installed
- Neutral baseColor / default style preserved
</success_criteria>

<output>
Create `.planning/phases/02-world-management/02-02-SUMMARY.md` when done
</output>
