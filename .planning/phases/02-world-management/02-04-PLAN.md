---
phase: 02-world-management
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/proxy.ts
  - src/components/layout/sidebar.tsx
  - src/app/(worlds)/worlds/[slug]/layout.tsx
  - src/app/(worlds)/worlds/[slug]/page.tsx
  - src/app/(worlds)/worlds/[slug]/world-detail-actions.tsx
autonomous: false
requirements: [UAT-6]
must_haves:
  truths:
    - "Visiting /worlds/[slug] as the owner shows the world detail shell"
    - "Visiting /worlds/[slug] for a world you do not own returns 404 (not 403)"
    - "An unauthenticated user hitting /worlds/[slug] is redirected to /login"
    - "The detail page shows name, description, privacy badge, slug, edit/delete actions, and a Phase 3 placeholder"
    - "The sidebar shows the world name as a contextual header and an 'Entities (Phase 3)' placeholder when inside /worlds/[slug]"
  artifacts:
    - path: "src/proxy.ts"
      provides: "auth redirect extended to /worlds paths"
      contains: "/worlds"
    - path: "src/components/layout/sidebar.tsx"
      provides: "world-context header + 'Entities (Phase 3)' placeholder section when on /worlds/[slug]"
      contains: "Entities (Phase 3)"
    - path: "src/app/(worlds)/worlds/[slug]/layout.tsx"
      provides: "RSC layout: await params, session guard, owner-scoped world fetch, 404 on miss"
      contains: "notFound"
    - path: "src/app/(worlds)/worlds/[slug]/page.tsx"
      provides: "RSC world detail shell with metadata, actions, and Phase 3 placeholder"
      contains: "getWorldBySlug"
    - path: "src/app/(worlds)/worlds/[slug]/world-detail-actions.tsx"
      provides: "client island owning Edit/Delete open state, composing controlled dialogs from 02-03"
      contains: "EditWorldDialog"
  key_links:
    - from: "src/app/(worlds)/worlds/[slug]/layout.tsx"
      to: "params Promise"
      via: "await params (Next.js 16)"
      pattern: "await params"
    - from: "src/app/(worlds)/worlds/[slug]/page.tsx"
      to: "getWorldBySlug"
      via: "owner-scoped fetch"
      pattern: "getWorldBySlug\\("
    - from: "src/proxy.ts"
      to: "/worlds redirect"
      via: "protected path check"
      pattern: "/worlds"
---

<objective>
Build world-scoped routing: extend proxy.ts to protect `/worlds`, add the `/worlds/[slug]` layout (owner-scoped fetch + 404 guard) and the world detail page shell with metadata, edit/delete actions, and a Phase 3 placeholder; extend the sidebar with world-context behavior.

Purpose: Establishes `/worlds/[slug]/...` as the root for all future world content and satisfies UAT-6 (private worlds inaccessible to unauthenticated users). Depends on 02-03 because the detail page reuses EditWorldDialog/DeleteWorldDialog.
Output: `src/proxy.ts` (modified), `src/components/layout/sidebar.tsx` (modified), `src/app/(worlds)/worlds/[slug]/layout.tsx`, `src/app/(worlds)/worlds/[slug]/page.tsx`, `src/app/(worlds)/worlds/[slug]/world-detail-actions.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-world-management/02-RESEARCH.md
@.planning/phases/02-world-management/02-PATTERNS.md
@.planning/phases/02-world-management/02-UI-SPEC.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend proxy.ts to protect /worlds and add the world layout guard</name>
  <files>src/proxy.ts, src/app/(worlds)/worlds/[slug]/layout.tsx</files>
  <read_first>
    - src/proxy.ts (current — protects only `/dashboard`; matcher is the broad negative-lookahead `/((?!_next/static|_next/image|favicon\.ico|api/).*)`)
    - src/app/(app)/layout.tsx (the session-guard + children-render layout pattern + Sidebar shell to mirror)
    - .planning/phases/02-world-management/02-PATTERNS.md (`proxy.ts` extension pattern; `worlds/[slug]/layout.tsx` section — async params, 404-not-403 pattern)
    - .planning/phases/02-world-management/02-RESEARCH.md (Pattern 2 async params; "World Layout Auth Guard" code example; Open Question 3 — protect /worlds in proxy)
    - src/lib/db/queries/worlds.ts (getWorldBySlug from 02-01)
    - node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md (async params is mandatory — `params` is a Promise; sync access throws)
  </read_first>
  <action>
    Modify `src/proxy.ts`: broaden the protected-path check so unauthenticated users are redirected to `/login` when `pathname.startsWith("/dashboard") || pathname.startsWith("/worlds")`. Introduce an `isProtected` boolean for clarity. Preserve the existing authenticated `/` → `/dashboard` redirect.

    DO NOT narrow the `config.matcher`. The current matcher `"/((?!_next/static|_next/image|favicon\.ico|api/).*)"` is a broad negative-lookahead that already routes every non-asset, non-API request — including all `/worlds/*` paths — through `proxy`. There is no positive-match allowlist of specific paths, so `/worlds` is in scope without any matcher edit. The verify step below proves this: it confirms the matcher still uses the negative-lookahead form `(?!...)` and does NOT contain a restrictive positive path list (e.g. a literal `"/dashboard"` matcher entry) that would exclude `/worlds`. The behavioral redirect itself (incognito → `/worlds/anything` → `/login`) is confirmed at the Task 3 checkpoint and in 02-VALIDATION.md UAT-6.

    Create `src/app/(worlds)/worlds/[slug]/layout.tsx` as an async RSC. Type props as `{ params: Promise<{ slug: string }>; children: React.ReactNode }`. `const { slug } = await params` (Next.js 16 — sync `params.slug` access throws; mandatory per AGENTS.md). Fetch session via `auth.api.getSession({ headers: await headers() })`; if null `redirect("/login")`. Call `getWorldBySlug(slug, session.user.id)`; if it returns null call `notFound()` from `next/navigation` — use `notFound()` (NOT redirect, NOT 403) so a non-owner cannot distinguish "does not exist" from "exists but not yours" (prevents slug enumeration, RESEARCH Security Domain). Because `(worlds)` is a separate route group from `(app)`, render the same shell AppLayout uses: a `flex h-screen overflow-hidden` wrapper containing `<Sidebar />` and `<main className="flex-1 overflow-y-auto">{children}</main>` (import `Sidebar` from `@/components/layout/sidebar`). Do not pass the fetched world down via context in Phase 2 — the page re-fetches it.
  </action>
  <verify>
    <automated>grep -q 'startsWith("/worlds")' src/proxy.ts && grep -Eq 'matcher.*\(\?!|\(\?!.*\)\.\*' src/proxy.ts && ! grep -Eq 'matcher:[^]]*"/dashboard' src/proxy.ts && grep -q 'await params' "src/app/(worlds)/worlds/[slug]/layout.tsx" && grep -q 'notFound' "src/app/(worlds)/worlds/[slug]/layout.tsx" && grep -q 'getWorldBySlug' "src/app/(worlds)/worlds/[slug]/layout.tsx" && ! grep -E 'params\.slug' "src/app/(worlds)/worlds/[slug]/layout.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.ts` redirects unauthenticated requests for paths starting with `/worlds` to `/login` (via `pathname.startsWith("/worlds")`)
    - `src/proxy.ts` matcher remains the broad negative-lookahead form and contains NO restrictive positive path allowlist (no literal `"/dashboard"`/path-list matcher entry) that would exclude `/worlds`
    - `layout.tsx` uses `const { slug } = await params` — no synchronous `params.slug` access
    - `layout.tsx` calls `getWorldBySlug(slug, session.user.id)` and `notFound()` when the world is null (404, not 403)
    - `layout.tsx` renders Sidebar + `main flex-1 overflow-y-auto` shell
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/worlds is auth-protected (matcher confirmed to cover it) and the world layout enforces owner-scoped 404 access control.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: World detail page shell + Edit/Delete client island</name>
  <files>src/app/(worlds)/worlds/[slug]/page.tsx, src/app/(worlds)/worlds/[slug]/world-detail-actions.tsx</files>
  <read_first>
    - .planning/phases/02-world-management/02-UI-SPEC.md (Surface Spec 7 World Detail Page Shell — breadcrumb, header row with Edit/Delete, description, metadata row, Separator, placeholder section; Copywriting Contract placeholder string)
    - .planning/phases/02-world-management/02-PATTERNS.md (`worlds/[slug]/page.tsx` section — async params, page structure/typography)
    - .planning/phases/02-world-management/02-RESEARCH.md (Open Question 1 — page shows metadata + edit/delete + Phase 3 placeholder; Pitfall 5 caching posture)
    - src/lib/db/queries/worlds.ts (getWorldBySlug from 02-01)
    - src/components/worlds/edit-world-dialog.tsx and delete-world-dialog.tsx (from 02-03 — reused here, controlled open state)
    - src/components/ui/separator.tsx and badge.tsx (from 02-02)
    - src/app/(app)/dashboard/page.tsx (typography/spacing reference: p-8, text-2xl font-semibold tracking-tight; `dynamic = "force-dynamic"` pattern)
  </read_first>
  <action>
    Create `src/app/(worlds)/worlds/[slug]/world-detail-actions.tsx` FIRST (`"use client"`). This is the client island that owns the Edit/Delete dialog open state (an RSC page cannot hold `useState`). Export a named `WorldDetailActions({ world }: { world: World })`. It manages local `editOpen`/`deleteOpen` state, renders the two action Buttons (Edit `variant="outline" size="sm"`; Delete `variant="outline" size="sm"` with `text-destructive`) in a `flex gap-2`, and renders the controlled `EditWorldDialog` (open=editOpen, onOpenChange=setEditOpen, world) and `DeleteWorldDialog` (open=deleteOpen, onOpenChange=setDeleteOpen, world) imported from `@/components/worlds/edit-world-dialog` and `@/components/worlds/delete-world-dialog` (02-03). The Edit button sets editOpen=true; the Delete button sets deleteOpen=true.

    Create `src/app/(worlds)/worlds/[slug]/page.tsx` as an async RSC. At module scope `export const dynamic = "force-dynamic"` (same caching posture as the dashboard — RESEARCH Pitfall 5; no `'use cache'`/`cacheTag` in this project, so the detail page must read fresh DB state to reflect edits/privacy toggles immediately). Props `{ params: Promise<{ slug: string }> }`; `const { slug } = await params`. Fetch session and `const world = await getWorldBySlug(slug, session.user.id)`; if null `notFound()` (defense in depth — the layout already guarded, but the page must not assume non-null). Container `p-8`.

    Structure top-to-bottom (UI-SPEC Surface Spec 7): (1) breadcrumb `text-sm text-muted-foreground` — a `Link` "Your Worlds" → `/dashboard`, then ` / {world.name}`; (2) header row `flex items-center justify-between` — world name `text-2xl font-semibold tracking-tight` on the left, and on the right the `<WorldDetailActions world={world} />` client island (owns the Edit/Delete buttons + dialogs); (3) description `text-sm text-muted-foreground max-w-prose mt-2 mb-6`; (4) metadata row `flex items-center gap-3 mb-8` — privacy `Badge variant="outline"` (Lock+"Private" / Globe+"Public") and slug `font-mono text-sm text-muted-foreground` showing `/worlds/{world.slug}`; (5) a `Separator`; (6) placeholder section `mt-8` centered `text-muted-foreground text-sm`: "Entities and content coming in Phase 3."

    Keep the page itself an RSC — only `world-detail-actions.tsx` is `"use client"`. Document the island approach in the SUMMARY.
  </action>
  <verify>
    <automated>test -f "src/app/(worlds)/worlds/[slug]/world-detail-actions.tsx" && grep -q 'EditWorldDialog' "src/app/(worlds)/worlds/[slug]/world-detail-actions.tsx" && grep -q '"use client"' "src/app/(worlds)/worlds/[slug]/world-detail-actions.tsx" && grep -q 'await params' "src/app/(worlds)/worlds/[slug]/page.tsx" && grep -q 'getWorldBySlug' "src/app/(worlds)/worlds/[slug]/page.tsx" && grep -q 'WorldDetailActions' "src/app/(worlds)/worlds/[slug]/page.tsx" && grep -q 'coming in Phase 3' "src/app/(worlds)/worlds/[slug]/page.tsx" && grep -q 'Your Worlds' "src/app/(worlds)/worlds/[slug]/page.tsx" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `world-detail-actions.tsx` exists, is `"use client"`, owns Edit/Delete open state, and composes the controlled `EditWorldDialog`/`DeleteWorldDialog` from 02-03
    - Page exports `dynamic = "force-dynamic"`, uses `const { slug } = await params` and `getWorldBySlug(slug, session.user.id)`
    - Renders breadcrumb (Link to /dashboard), world name heading, `WorldDetailActions` island, description, privacy Badge + `font-mono` slug, a Separator, and the placeholder "Entities and content coming in Phase 3."
    - Page itself stays an RSC (no `useState`/`"use client"` in page.tsx)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>World detail shell renders owner-only with metadata, working edit/delete actions via a client island, and a Phase 3 placeholder; build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sidebar world-context header + 'Entities (Phase 3)' placeholder</name>
  <files>src/components/layout/sidebar.tsx</files>
  <read_first>
    - src/components/layout/sidebar.tsx (current — `"use client"`, `usePathname`, locked widths 240px/56px, h-14 header; nav items list; collapse toggle)
    - .planning/phases/02-world-management/02-UI-SPEC.md (Surface Spec 7 "Sidebar behavior in world context" — world name as contextual header, "Entities (Phase 3)" placeholder section in `text-xs text-muted-foreground`; Sidebar dimensions locked)
    - src/app/(worlds)/worlds/[slug]/layout.tsx (Task 1 — renders this Sidebar inside the world route group)
  </read_first>
  <action>
    Extend `src/components/layout/sidebar.tsx` (keep `"use client"`; do NOT change the locked widths, h-14 header height, transition, sign-out, or collapse toggle). Use the existing `usePathname()` to detect world context: derive `const worldSlug = pathname.match(/^\/worlds\/([^/]+)/)?.[1]` (truthy only on `/worlds/[slug]/...`).

    When `worldSlug` is present AND the sidebar is expanded (`sidebarOpen`), render — below the existing primary nav items — a world-context block: the world slug/name shown as a contextual header (`px-2 text-sm font-medium text-foreground truncate`, using the slug as the visible label since the sidebar is a client component without the world record; UI-SPEC accepts the world identifier here), followed immediately by a placeholder nav section reading "Entities (Phase 3)" styled `px-2 text-xs text-muted-foreground` (UI-SPEC Surface Spec 7). This is a non-interactive placeholder — no Link, no real items. When collapsed, omit the text block (consistent with how the existing nav hides labels when collapsed). When not on a `/worlds/[slug]` path, render nothing extra (the dashboard sidebar is unchanged).

    Do not introduce new dependencies; reuse the existing lucide imports if an icon is wanted, but an icon is optional per UI-SPEC.
  </action>
  <verify>
    <automated>grep -q 'Entities (Phase 3)' src/components/layout/sidebar.tsx && grep -Eq '/worlds/' src/components/layout/sidebar.tsx && grep -q 'usePathname' src/components/layout/sidebar.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar shows a world-context header (world slug/name) when `pathname` matches `/worlds/[slug]` and the sidebar is expanded
    - Sidebar renders an "Entities (Phase 3)" placeholder section in `text-xs text-muted-foreground` in world context
    - Locked dimensions (240px/56px, h-14), sign-out, and collapse toggle are unchanged
    - No world-context block rendered outside `/worlds/[slug]` paths
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sidebar communicates world context with a contextual header and a Phase 3 entities placeholder per UI-SPEC Surface Spec 7, without altering locked Phase 1 dimensions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify world-scoped routing and access control in the browser</name>
  <what-built>proxy.ts now redirects unauthenticated `/worlds` requests to login; the `/worlds/[slug]` layout enforces owner-scoped access with a 404 for non-owned/nonexistent worlds; the detail page renders world metadata + edit/delete + a Phase 3 placeholder; the sidebar shows world-context header + Entities (Phase 3) placeholder.</what-built>
  <how-to-verify>
    1. `npm run dev`, sign in, create a world (or use an existing one), then click its card → confirm you land on `/worlds/{slug}` showing breadcrumb, name, description, privacy badge, slug, and "Entities and content coming in Phase 3."
    2. Sidebar context: while on `/worlds/{slug}`, confirm the sidebar shows the world context header and an "Entities (Phase 3)" placeholder; return to /dashboard and confirm that block disappears.
    3. Edit from detail page: click Edit → change name → Save → confirm it updates.
    4. Delete from detail page: click Delete → confirm AlertDialog → "Delete world" → confirm redirect back to /dashboard and the world is gone.
    5. Owner-scope 404: visit `/worlds/some-slug-you-do-not-own` (or a random nonexistent slug) → confirm a 404 (not-found), NOT a 403 and NOT another user's data.
    6. Unauthenticated guard (UAT-6): open an incognito window (signed out) and visit `/worlds/anything` → confirm redirect to `/login`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated request → /worlds route | proxy.ts + layout must block before any world data is read |
| slug in URL → DB lookup | attacker-supplied slug must never return another user's world |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | unauthenticated /worlds/[slug] access | mitigate | proxy.ts redirects `startsWith("/worlds")` to /login; matcher confirmed (verify) to be the broad negative-lookahead that covers /worlds; layout re-checks session (belt-and-suspenders) — satisfies UAT-6 |
| T-02-10 | Information Disclosure | slug enumeration to discover other users' worlds | mitigate | `getWorldBySlug(slug, session.user.id)` is owner-scoped; layout returns `notFound()` (404, not 403) so existence is not confirmable |
| T-02-11 | Elevation of Privilege | accessing another user's world detail | mitigate | Both layout and page fetch with ownerId scope and call `notFound()` on null — no cross-user read path |
| T-02-SC | Tampering | npm installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` exit 0
- proxy matcher confirmed (verify gate) to cover /worlds (negative-lookahead, no restrictive allowlist)
- Human checkpoint (Task 4) confirms detail render, sidebar world-context, edit/delete from detail, owner-scope 404, and unauthenticated redirect
- Covers UAT-6 (private world not accessible to unauthenticated users) and UI-SPEC Surface Spec 7 (sidebar world context)
</verification>

<success_criteria>
- `/worlds` is auth-protected at proxy (matcher coverage verified) + layout
- Owner-scoped 404 prevents cross-user access and slug enumeration
- World detail shell renders metadata + actions (client island) + Phase 3 placeholder
- Sidebar shows world-context header + Entities (Phase 3) placeholder
- Build passes; human verification approved
</success_criteria>

<output>
Create `.planning/phases/02-world-management/02-04-SUMMARY.md` when done
</output>
