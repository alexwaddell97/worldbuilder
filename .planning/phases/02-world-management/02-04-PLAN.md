---
phase: 02-world-management
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/proxy.ts
  - src/app/(worlds)/worlds/[slug]/layout.tsx
  - src/app/(worlds)/worlds/[slug]/page.tsx
autonomous: false
requirements: [UAT-6]
must_haves:
  truths:
    - "Visiting /worlds/[slug] as the owner shows the world detail shell"
    - "Visiting /worlds/[slug] for a world you do not own returns 404 (not 403)"
    - "An unauthenticated user hitting /worlds/[slug] is redirected to /login"
    - "The detail page shows name, description, privacy badge, slug, edit/delete actions, and a Phase 3 placeholder"
  artifacts:
    - path: "src/proxy.ts"
      provides: "auth redirect extended to /worlds paths"
      contains: "/worlds"
    - path: "src/app/(worlds)/worlds/[slug]/layout.tsx"
      provides: "RSC layout: await params, session guard, owner-scoped world fetch, 404 on miss"
      contains: "notFound"
    - path: "src/app/(worlds)/worlds/[slug]/page.tsx"
      provides: "RSC world detail shell with metadata, actions, and Phase 3 placeholder"
      contains: "getWorldBySlug"
  key_links:
    - from: "src/app/(worlds)/worlds/[slug]/layout.tsx"
      to: "params Promise"
      via: "await params (Next.js 16)"
      pattern: "await params"
    - from: "src/app/(worlds)/worlds/[slug]/page.tsx"
      to: "getWorldBySlug"
      via: "owner-scoped fetch"
      pattern: "getWorldBySlug\\("
    - from: "src/proxy.ts"
      to: "/worlds redirect"
      via: "protected path check"
      pattern: "/worlds"
---

<objective>
Build world-scoped routing: extend proxy.ts to protect `/worlds`, add the `/worlds/[slug]` layout (owner-scoped fetch + 404 guard) and the world detail page shell with metadata, edit/delete actions, and a Phase 3 placeholder.

Purpose: Establishes `/worlds/[slug]/...` as the root for all future world content and satisfies UAT-6 (private worlds inaccessible to unauthenticated users). Depends on 02-03 because the detail page reuses EditWorldDialog/DeleteWorldDialog.
Output: `src/proxy.ts` (modified), `src/app/(worlds)/worlds/[slug]/layout.tsx`, `src/app/(worlds)/worlds/[slug]/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-world-management/02-RESEARCH.md
@.planning/phases/02-world-management/02-PATTERNS.md
@.planning/phases/02-world-management/02-UI-SPEC.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend proxy.ts to protect /worlds and add the world layout guard</name>
  <files>src/proxy.ts, src/app/(worlds)/worlds/[slug]/layout.tsx</files>
  <read_first>
    - src/proxy.ts (current — protects only `/dashboard`; do NOT change the `matcher` config regex)
    - src/app/(app)/layout.tsx (the session-guard + children-render layout pattern + Sidebar shell to mirror)
    - .planning/phases/02-world-management/02-PATTERNS.md (`proxy.ts` extension pattern; `worlds/[slug]/layout.tsx` section — async params, 404-not-403 pattern)
    - .planning/phases/02-world-management/02-RESEARCH.md (Pattern 2 async params; "World Layout Auth Guard" code example; Open Question 3 — protect /worlds in proxy)
    - src/lib/db/queries/worlds.ts (getWorldBySlug from 02-01)
    - node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md (async params is mandatory — `params` is a Promise; sync access throws)
  </read_first>
  <action>
    Modify `src/proxy.ts`: broaden the protected-path check so unauthenticated users are redirected to `/login` when `pathname.startsWith("/dashboard") || pathname.startsWith("/worlds")`. Introduce an `isProtected` boolean for clarity. Do NOT modify the `config.matcher` regex (it already covers these paths). Preserve the existing authenticated `/` → `/dashboard` redirect.

    Create `src/app/(worlds)/worlds/[slug]/layout.tsx` as an async RSC. Type props as `{ params: Promise<{ slug: string }>; children: React.ReactNode }`. `const { slug } = await params` (Next.js 16 — sync `params.slug` access throws; mandatory per AGENTS.md). Fetch session via `auth.api.getSession({ headers: await headers() })`; if null `redirect("/login")`. Call `getWorldBySlug(slug, session.user.id)`; if it returns null call `notFound()` from `next/navigation` — use `notFound()` (NOT redirect, NOT 403) so a non-owner cannot distinguish "does not exist" from "exists but not yours" (prevents slug enumeration, RESEARCH Security Domain). Because `(worlds)` is a separate route group from `(app)`, render the same shell AppLayout uses: a `flex h-screen overflow-hidden` wrapper containing `<Sidebar />` and `<main className="flex-1 overflow-y-auto">{children}</main>` (import `Sidebar` from `@/components/layout/sidebar`). Do not pass the fetched world down via context in Phase 2 — the page re-fetches it.
  </action>
  <verify>
    <automated>grep -q '/worlds' src/proxy.ts && grep -q 'await params' "src/app/(worlds)/worlds/[slug]/layout.tsx" && grep -q 'notFound' "src/app/(worlds)/worlds/[slug]/layout.tsx" && grep -q 'getWorldBySlug' "src/app/(worlds)/worlds/[slug]/layout.tsx" && ! grep -E 'params\.slug' "src/app/(worlds)/worlds/[slug]/layout.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.ts` redirects unauthenticated requests for paths starting with `/worlds` to `/login`; matcher regex unchanged
    - `layout.tsx` uses `const { slug } = await params` — no synchronous `params.slug` access
    - `layout.tsx` calls `getWorldBySlug(slug, session.user.id)` and `notFound()` when the world is null (404, not 403)
    - `layout.tsx` renders Sidebar + `main flex-1 overflow-y-auto` shell
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/worlds is auth-protected and the world layout enforces owner-scoped 404 access control.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: World detail page shell with metadata, actions, and Phase 3 placeholder</name>
  <files>src/app/(worlds)/worlds/[slug]/page.tsx</files>
  <read_first>
    - .planning/phases/02-world-management/02-UI-SPEC.md (Surface Spec 7 World Detail Page Shell — breadcrumb, header row with Edit/Delete, description, metadata row, Separator, placeholder section; Copywriting Contract placeholder string)
    - .planning/phases/02-world-management/02-PATTERNS.md (`worlds/[slug]/page.tsx` section — async params, page structure/typography)
    - .planning/phases/02-world-management/02-RESEARCH.md (Open Question 1 — page shows metadata + edit/delete + Phase 3 placeholder)
    - src/lib/db/queries/worlds.ts (getWorldBySlug from 02-01)
    - src/components/worlds/edit-world-dialog.tsx and delete-world-dialog.tsx (from 02-03 — reused here, controlled open state)
    - src/components/ui/separator.tsx and badge.tsx (from 02-02)
    - src/app/(app)/dashboard/page.tsx (typography/spacing reference: p-8, text-2xl font-semibold tracking-tight)
  </read_first>
  <action>
    Create `src/app/(worlds)/worlds/[slug]/page.tsx` as an async RSC. Props `{ params: Promise<{ slug: string }> }`; `const { slug } = await params`. Fetch session and `const world = await getWorldBySlug(slug, session.user.id)`; if null `notFound()` (defense in depth — the layout already guarded, but the page must not assume non-null). Container `p-8`.

    Structure top-to-bottom (UI-SPEC Surface Spec 7): (1) breadcrumb `text-sm text-muted-foreground` — a `Link` "Your Worlds" → `/dashboard`, then ` / {world.name}`; (2) header row `flex items-center justify-between` — world name `text-2xl font-semibold tracking-tight` on the left, and on the right (`gap-2`) an Edit button (`variant="outline" size="sm"`) and a Delete button (`variant="outline" size="sm"` with `text-destructive`); (3) description `text-sm text-muted-foreground max-w-prose mt-2 mb-6`; (4) metadata row `flex items-center gap-3 mb-8` — privacy `Badge variant="outline"` (Lock+"Private" / Globe+"Public") and slug `font-mono text-sm text-muted-foreground` showing `/worlds/{world.slug}`; (5) a `Separator`; (6) placeholder section `mt-8` centered `text-muted-foreground text-sm`: "Entities and content coming in Phase 3."

    Because EditWorldDialog and DeleteWorldDialog are client components with controlled `open`/`onOpenChange`, wrap the Edit/Delete buttons + dialogs in a small client component island (create it inline as `src/app/(worlds)/worlds/[slug]/page.tsx` cannot itself hold useState). Add a `"use client"` sibling — name it within this file's directory, e.g. a `WorldDetailActions` component file is acceptable, OR reuse the dialogs by passing the world and rendering a thin client wrapper. Keep the page itself an RSC; only the action buttons island is `"use client"`. The simplest path: create the island in the same plan if needed — but prefer composing the existing controlled EditWorldDialog/DeleteWorldDialog from 02-03 inside a tiny client wrapper that owns the open state for both. Document the chosen approach in the summary.
  </action>
  <verify>
    <automated>grep -q 'await params' "src/app/(worlds)/worlds/[slug]/page.tsx" && grep -q 'getWorldBySlug' "src/app/(worlds)/worlds/[slug]/page.tsx" && grep -q 'coming in Phase 3' "src/app/(worlds)/worlds/[slug]/page.tsx" && grep -q 'Your Worlds' "src/app/(worlds)/worlds/[slug]/page.tsx" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Page uses `const { slug } = await params` and `getWorldBySlug(slug, session.user.id)`
    - Renders breadcrumb (Link to /dashboard), world name heading, description, privacy Badge + `font-mono` slug, a Separator, and the placeholder "Entities and content coming in Phase 3."
    - Edit/Delete actions render via a `"use client"` island composing the controlled dialogs from 02-03 (page itself stays RSC)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>World detail shell renders owner-only with metadata, working edit/delete actions, and a Phase 3 placeholder; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify world-scoped routing and access control in the browser</name>
  <what-built>proxy.ts now redirects unauthenticated `/worlds` requests to login; the `/worlds/[slug]` layout enforces owner-scoped access with a 404 for non-owned/nonexistent worlds; the detail page renders world metadata + edit/delete + a Phase 3 placeholder.</what-built>
  <how-to-verify>
    1. `npm run dev`, sign in, create a world (or use an existing one), then click its card → confirm you land on `/worlds/{slug}` showing breadcrumb, name, description, privacy badge, slug, and "Entities and content coming in Phase 3."
    2. Edit from detail page: click Edit → change name → Save → confirm it updates.
    3. Delete from detail page: click Delete → confirm AlertDialog → "Delete world" → confirm redirect back to /dashboard and the world is gone.
    4. Owner-scope 404: visit `/worlds/some-slug-you-do-not-own` (or a random nonexistent slug) → confirm a 404 (not-found), NOT a 403 and NOT another user's data.
    5. Unauthenticated guard (UAT-6): open an incognito window (signed out) and visit `/worlds/anything` → confirm redirect to `/login`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated request → /worlds route | proxy.ts + layout must block before any world data is read |
| slug in URL → DB lookup | attacker-supplied slug must never return another user's world |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | unauthenticated /worlds/[slug] access | mitigate | proxy.ts redirects `startsWith("/worlds")` to /login; layout re-checks session (belt-and-suspenders) — satisfies UAT-6 |
| T-02-10 | Information Disclosure | slug enumeration to discover other users' worlds | mitigate | `getWorldBySlug(slug, session.user.id)` is owner-scoped; layout returns `notFound()` (404, not 403) so existence is not confirmable |
| T-02-11 | Elevation of Privilege | accessing another user's world detail | mitigate | Both layout and page fetch with ownerId scope and call `notFound()` on null — no cross-user read path |
| T-02-SC | Tampering | npm installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npm run build` exits 0
- Human checkpoint (Task 3) confirms detail render, edit/delete from detail, owner-scope 404, and unauthenticated redirect
- Covers UAT-6 (private world not accessible to unauthenticated users)
</verification>

<success_criteria>
- `/worlds` is auth-protected at proxy + layout
- Owner-scoped 404 prevents cross-user access and slug enumeration
- World detail shell renders metadata + actions + Phase 3 placeholder
- Build passes; human verification approved
</success_criteria>

<output>
Create `.planning/phases/02-world-management/02-04-SUMMARY.md` when done
</output>
